# ── Web framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
httpx
pydantic>=2.7.4
python-dotenv
sse-starlette
redis>=5.0.0

# ── AutoGen 0.4.x (async, MCP-native) ────────────────────────────────────────
# autogen-agentchat  — AssistantAgent, run_stream(), message types
# autogen-ext[openai] — OpenAIChatCompletionClient (works with OpenRouter too)
# autogen-ext[mcp]   — McpWorkbench, StreamableHttpServerParams
#
# NOTE: pyautogen 0.2.x (classic AssistantAgent/UserProxyAgent) is NOT used
# anymore; remove the pyautogen==0.2.35 line from the Dockerfile as well.
autogen-agentchat>=0.4.0
autogen-ext[openai,mcp]>=0.4.0

# ── LangGraph agent (non-chat routes use this) ────────────────────────────────
mcp>=1.0.0,<2.0.0
langchain-mcp-adapters==0.2.1
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-mistralai>=0.2.0
aiobreaker>=1.0.0
tenacity>=8.0.0
